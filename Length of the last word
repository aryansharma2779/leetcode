class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        if len(s)==0:
            return 0
        else:
            a=s.split()
            if len(a)==0:
                return 0
                
            return len(a[-1])
